<%= form_for [commentable, Comment.new], remote: true do |f| %>
  <div class="comment-input-container">
    <%= f.text_area :body, class: "comment-textarea", placeholder: "Add a comment" %>
    <%= f.submit "Post Comment", class: "btn comment-post-btn" %>
  </div>
<% end %>
